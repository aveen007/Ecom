
@model IEnumerable<Ecom.Models.CategoryViewModel>
@{
    ViewData["Title"] = "About";
}


<div class="container-fluid pt-5">
    <h2 class="section-title position-relative text-uppercase mx-xl-5 mb-4"><span class="bg-secondary pr-3">Categories</span></h2>

    <p>
        @await Html.PartialAsync("_SorterPartial")

        @*@if ((await AuthorizationService.AuthorizeAsync(User, "Admin")).Succeeded)*@
   


    </p>
    <div class="row px-xl-5 pb-3">
        @foreach (var category in Model)
        {
            <!-- Categories Start -->
            <div class="col-lg-3 col-md-4 col-sm-6 pb-1">
                <a class="text-decoration-none" href=@Url.Action("Shop", "Categories", new {id = category.Id})>
                    <div class="cat-item d-flex align-items-center mb-4">
                        <div class="overflow-hidden" style="width: 100px; height: 100px;">
                            <img class="img-fluid" src="~/img/cat-1.jpg" alt="">
                        </div>
                        <div class="flex-fill pl-3">
                            <a href=@Url.Action("Shop", "Categories", new {id = category.Id}) class="nav-item nav-link">@category.Name</a>

                          
                            <small class="text-body">100 Products</small>
                        </div>
                    </div>
                </a>
            </div>

        }
    </div>
</div>
@await Html.PartialAsync("_PaginationPartial")

<!-- Categories End -->

@*<div class="navbar-nav w-100">
    <div class="nav-item dropdown dropright">
        @foreach (var category in ViewBag.Categories)
        {
            <a href=@Url.Action("Shop", "Categories", new {id = category.Id}) class="nav-item nav-link">@category.Name</a>
        }
    </div>
</div>*@